<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heima.mapper.VpnAdminMapper">

    <select id="findByAccount" resultType="com.heima.entity.VpnAdmin">
        SELECT id, account, password, name, created_at, updated_at
        FROM vpn_admin WHERE account = #{account} LIMIT 1
    </select>

    <select id="findById" resultType="com.heima.entity.VpnAdmin">
        SELECT id, account, password, name, created_at, updated_at
        FROM vpn_admin WHERE id = #{id} LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO vpn_admin (account, password, name, created_at, updated_at)
        VALUES (#{account}, #{password}, #{name}, NOW(), NOW())
    </insert>

    <update id="updatePassword">
        UPDATE vpn_admin SET password = #{password}, updated_at = NOW() WHERE id = #{id}
    </update>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM vpn_admin
    </select>
</mapper>
