<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heima.mapper.VpnAdminSessionMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO vpn_admin_session (admin_id, token, expire_at, created_at)
        VALUES (#{adminId}, #{token}, #{expireAt}, NOW())
    </insert>

    <select id="findByToken" resultType="com.heima.entity.VpnAdminSession">
        SELECT id, admin_id, token, expire_at, created_at
        FROM vpn_admin_session
        WHERE token = #{token} AND expire_at &gt; NOW()
        LIMIT 1
    </select>

    <delete id="deleteByToken">
        DELETE FROM vpn_admin_session WHERE token = #{token}
    </delete>
</mapper>
